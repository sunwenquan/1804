from chengji import scoreclass A:    def __init__(self):        print('我回自动执行')    def test_01(self):        act_result = score(90)  # A        ext_result = 'A'        if act_result == ext_result:            print('测试通过')        else:            raise Exception('测试失败，{0} != {1}'.format(act_result, ext_result))    def test_02(self):        act_result = score(80)  # A        ext_result = 'C'        if act_result == ext_result:            print('测试通过')        else:            raise Exception('测试失败，{0} != {1}'.format(act_result, ext_result))    def __del__(self):        print('运行结束了')if __name__ == '__main__':    a = A()    a.test_01()    a.test_02()"""1. 能否自动根据函数名调用方法2. 能否优化一个输出结果，例如：有几个通过，有几个失败3. 在执行方法之前，自动执行某个方法，在执行方法之后自动执行某个方法4. 每个测试方法执行后都自动执行某个方？"""